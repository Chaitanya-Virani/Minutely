# Test-only dependencies. NOT installed in the production image — keep
# backend/requirements.txt at exactly 8 packages. Install locally / in CI with:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
python-multipart>=0.0.9
